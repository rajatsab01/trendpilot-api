services:
  - type: web
    name: trendpilot
    env: node
    region: oregon
    plan: free

    buildCommand: |
      npm install
      npm run build

    startCommand: node dist/index.js

    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
